import QtQuick 1.1

Item {
    id: stats

    property string name: "Search for secret doors"
    property string desc: "Search for secret doors in the vicinity"

    function useAbility( user, target ) {
    }
    Component.onCompleted: {
    }
}
