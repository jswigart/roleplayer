import QtQuick 1.1

Item {
    id: stats

    property string name: "Search for traps"
    property string desc: "Search for traps in the vicinity"

    function useAbility( user, target ) {
    }
    Component.onCompleted: {
    }
}
