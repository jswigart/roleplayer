import QtQuick 1.1

Item {
    id: stats

    property string name: "Cast Spell"
    property string desc: "Cast a spell on a target"

    function useAbility( user, target ) {
    }
    Component.onCompleted: {
    }
}
