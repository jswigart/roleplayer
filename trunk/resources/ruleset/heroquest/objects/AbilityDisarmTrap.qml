import QtQuick 1.1

Item {
    id: stats

    property string name: "Disarm Trap"
    property string desc: "Disarm a nearby trap"

    function useAbility( user, target ) {
    }
    Component.onCompleted: {
    }
}
