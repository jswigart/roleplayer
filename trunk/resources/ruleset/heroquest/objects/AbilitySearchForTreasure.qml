import QtQuick 1.1

Item {
    id: stats

    property string name: "Search for treasure"
    property string desc: "Search for treasure in the current room or corridor"

    function useAbility( user, target ) {
    }
    Component.onCompleted: {
    }
}
